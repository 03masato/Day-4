# CSSが反映されない問題の解決方法

## 現在の状態確認

✅ 開発サーバー: 起動中（ポート8000）
✅ main.js: 正しく読み込まれている
✅ SCSSファイル: 正しくコンパイルされている
✅ ビルド: 正常に完了

## 解決手順

### 1. ブラウザのハードリロード

**Windows:**
- `Ctrl + Shift + R`
- または `Ctrl + F5`

**Mac:**
- `Cmd + Shift + R`

### 2. ブラウザのキャッシュをクリア

1. 開発者ツールを開く（F12）
2. ネットワークタブを開く
3. 「キャッシュの無効化」にチェックを入れる
4. ページをリロード

### 3. 開発サーバーを再起動

```bash
# 現在のサーバーを停止（Ctrl+C）
# 再度起動
npm run dev
```

### 4. ブラウザのコンソールを確認

開発者ツール（F12）の「Console」タブでエラーがないか確認してください。

### 5. ネットワークタブで確認

開発者ツールの「Network」タブで：
- `main.js` が読み込まれているか
- CSSファイル（`style-*.css`）が読み込まれているか
- ステータスコードが200（成功）か確認

## 確認すべきポイント

### HTMLファイル
```html
<script type="module" src="/src/main.js"></script>
```
✅ この行が`</body>`の前に含まれていることを確認

### main.js
```javascript
import './scss/main.scss';
```
✅ SCSSが正しくインポートされていることを確認

### 開発サーバーのURL
- 正しいURL: `http://localhost:8000`
- `http://127.0.0.1:8000` でも動作します

## それでも解決しない場合

### 方法1: ビルドして確認

```bash
npm run build
npm run preview
```

ビルド後のファイルで確認してください。

### 方法2: シークレットモードで確認

ブラウザのシークレットモード（プライベートモード）で開いて確認してください。

### 方法3: 別のブラウザで確認

Chrome、Firefox、Safariなど、別のブラウザで確認してください。

## よくある問題と解決策

### 問題: 開発サーバーでCSSが反映されない

**原因**: ブラウザのキャッシュ

**解決策**: 
1. ハードリロード（`Ctrl+Shift+R`）
2. キャッシュをクリア
3. シークレットモードで確認

### 問題: コンソールにエラーが表示される

**確認事項**:
- `main.js`のパスが正しいか
- SCSSファイルのパスが正しいか
- ファイルが存在するか

### 問題: ネットワークタブでCSSが読み込まれていない

**確認事項**:
- 開発サーバーが起動しているか
- `main.js`が正しく読み込まれているか
- SCSSのインポートパスが正しいか

## 現在の設定確認

- ✅ `vite.config.js`: `base: './'` 設定済み
- ✅ `index.html`: `<script type="module" src="/src/main.js"></script>` 含まれている
- ✅ `src/main.js`: SCSSをインポートしている
- ✅ `src/scss/main.scss`: すべてのSCSSファイルを`@use`で読み込んでいる

すべての設定は正しいです。ブラウザのキャッシュをクリアして、ハードリロードを試してください。

